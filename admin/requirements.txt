# MeshPrint - Python 依赖清单
# 安装命令: pip install -r requirements.txt

# 核心框架
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6

# HTTP 客户端（用于API调用）
requests>=2.28.0

# 图像处理（可选，用于打印图片预览）
Pillow>=10.0.0
